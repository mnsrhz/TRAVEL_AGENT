# Copy these values into Streamlit Cloud secrets or a local .streamlit/secrets.toml.
# Do not commit real API keys.

OPENAI_API_KEY = ""
SERPAPI_API_KEY = ""
TAVILY_API_KEY = ""
GOOGLE_MAPS_API_KEY = ""

# false or omitted means strict live mode.
# true allows labeled demo fallbacks when live keys are missing or live APIs fail.
ALLOW_DEMO_FALLBACKS = "true"
